# TradeIQ - copy to .env and fill values
# Database: In Supabase click "Connect" -> copy the URI (Session mode port 5432 or Direct).
# Use the EXACT string from the dashboard (host can be pooler or db.REF.supabase.co).
# If you get "not a valid url": password has special chars - encode in .env: @=%40 #=%23 :=%3A /=%2F
# If you get "Tenant or user not found": use Session mode (port 5432) or Direct from Connect, not Transaction (6543).
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@host:5432/postgres

# Django
DJANGO_SECRET_KEY=your-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Bluesky (Appendix B) - Settings -> App Passwords
BLUESKY_HANDLE=tradeiq-analyst.bsky.social
BLUESKY_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
